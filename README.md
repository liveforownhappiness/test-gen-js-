# test-gen-js

> 🧪 Auto-generate test boilerplate code for JavaScript/TypeScript, React, and React Native projects

[![npm version](https://badge.fury.io/js/test-gen-js.svg)](https://badge.fury.io/js/test-gen-js)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

---

## 🎯 목표 (Goal)

> **"테스트 0개 → 기본 테스트라도 있는 상태"**

이 라이브러리는 완벽한 테스트 자동화가 아닌, **테스트 작성의 진입 장벽을 낮추는 것**을 목표로 합니다.

### 이 도구가 해결하는 문제

```
✅ "테스트 파일 만들기 귀찮아서 안 했어요" → 보일러플레이트 자동 생성
✅ "어떻게 시작해야 할지 모르겠어요" → 시작점 제공
✅ "렌더링 기본 테스트도 없어요" → 최소한의 안전망 제공
```

### 이 도구가 해결하지 못하는 문제

```
❌ 비즈니스 로직 테스트 자동 생성 → 개발자가 직접 작성 필요
❌ 완벽한 테스트 커버리지 → 불가능
```

### 핵심 가치

- **보일러플레이트 생성기**: 반복되는 테스트 코드 구조를 자동으로 생성
- **시작점 제공**: 빈 테스트 파일이 아닌, 실행 가능한 기본 테스트 제공
- **학습 도구**: 생성된 코드를 통해 테스트 작성법 학습 가능

---

## 📦 설치 (Installation)

```bash
# npm
npm install -g test-gen-js

# 또는 npx로 바로 사용
npx test-gen-js generate src/components/Button.tsx
```

---

## 🚀 사용법 (Usage)

### 기본 사용

```bash
# 단일 파일 테스트 생성
test-gen-js generate src/components/Button.tsx

# 또는 단축 명령어
tgjs g src/components/Button.tsx
```

### 옵션

```bash
# 스냅샷 테스트 포함
test-gen-js generate src/components/Card.tsx --snapshot

# 커스텀 출력 경로
test-gen-js generate src/components/Button.tsx --output __tests__/Button.test.tsx

# 디렉토리 전체 스캔
test-gen-js scan src/components

# 미리보기 (파일 생성 없이)
test-gen-js scan src/components --dry-run
```

---

## 📊 지원 범위 (Supported)

| 타입 | 지원 여부 | 테스트 프레임워크 |
|------|----------|-----------------|
| JavaScript 함수 | ✅ | Jest |
| TypeScript 함수 | ✅ | Jest |
| React 컴포넌트 | ✅ | Jest + React Testing Library |
| React Native 컴포넌트 | ✅ | Jest + React Native Testing Library |
| Node.js 모듈 | 🔜 (v0.2) | Jest |
| Vue 컴포넌트 | 🔜 (플러그인) | Vitest |
| Angular 컴포넌트 | 🔜 (플러그인) | Jasmine |

---

## 🗺️ 로드맵 (Roadmap)

### 1단계: MVP (v0.1.x) - 현재

> **최소 기능 제품 (Minimum Viable Product)**

- [x] 프로젝트 구조 설정
- [ ] AST 파서 구현 (Babel 기반)
- [ ] 기본 테스트 생성기
  - [ ] JavaScript/TypeScript 함수
  - [ ] React 컴포넌트
  - [ ] React Native 컴포넌트
- [ ] CLI 인터페이스
- [ ] 기본 템플릿 시스템

**목표**: 단일 파일에서 기본 테스트 보일러플레이트 생성

---

### 2단계: 확장 (v0.2.x)

> **기능 확장 및 개선**

- [ ] Node.js 백엔드 지원
  - [ ] Express 라우트 핸들러
  - [ ] Fastify 핸들러
- [ ] Mock 자동 생성
  - [ ] API 호출 Mock
  - [ ] 외부 모듈 Mock
- [ ] 프로젝트 설정 파일 지원
  - [ ] `.testgenrc.js` 설정 파일
  - [ ] Prettier/ESLint 연동
- [ ] 디렉토리 스캔 및 일괄 생성
- [ ] Watch 모드 (파일 변경 감지)
- [ ] 커스텀 템플릿 지원

**목표**: 프로젝트 전체 테스트 생성 및 설정 커스터마이징

---

### 3단계: 플러그인 시스템 (v0.3.x+)

> **확장 가능한 아키텍처**

```
test-gen-js (코어)
├── @test-gen-js/react        ← React 플러그인
├── @test-gen-js/react-native ← React Native 플러그인
├── @test-gen-js/node         ← Node.js 플러그인
├── @test-gen-js/vue          ← Vue 플러그인 (커뮤니티)
└── @test-gen-js/angular      ← Angular 플러그인 (커뮤니티)
```

- [ ] 플러그인 아키텍처 설계
- [ ] 플러그인 API 정의
- [ ] 커뮤니티 플러그인 가이드
- [ ] VS Code 확장 개발
- [ ] AI 통합 (선택적)
  - [ ] 더 스마트한 테스트 케이스 제안
  - [ ] 비즈니스 로직 테스트 보조

**목표**: 모든 JavaScript 생태계 지원 및 커뮤니티 확장

---

## 🔧 작동 원리 (How It Works)

### AST (Abstract Syntax Tree) 파싱

```
┌─────────────────────────────────────────────────────────────────┐
│  1. 입력: Button.tsx                                            │
├─────────────────────────────────────────────────────────────────┤
│  const Button = ({ title, onPress }) => {                       │
│    return (                                                     │
│      <TouchableOpacity onPress={onPress}>                       │
│        <Text>{title}</Text>                                     │
│      </TouchableOpacity>                                        │
│    );                                                           │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. AST 분석                                                    │
├─────────────────────────────────────────────────────────────────┤
│  {                                                              │
│    name: "Button",                                              │
│    type: "FunctionComponent",                                   │
│    props: ["title", "onPress"],                                 │
│    events: ["onPress"],                                         │
│    hooks: []                                                    │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. 출력: Button.test.tsx                                       │
├─────────────────────────────────────────────────────────────────┤
│  describe('Button', () => {                                     │
│    it('renders correctly', () => { ... });                      │
│    it('calls onPress when pressed', () => { ... });             │
│  });                                                            │
└─────────────────────────────────────────────────────────────────┘
```

### 자동화 범위

| 영역 | 자동화 가능 | 비고 |
|------|------------|------|
| import 문 | ✅ 100% | 패턴 분석 |
| Mock 설정 | ✅ 70% | import 분석 |
| defaultProps | ✅ 80% | 타입 분석 |
| 렌더링 테스트 | ✅ 100% | 항상 동일 |
| 이벤트 테스트 | ✅ 60% | 핸들러 분석 |
| **비즈니스 로직** | ❌ 0% | **개발자 필수** |

---

## 📁 프로젝트 구조 (Project Structure)

```
test-gen-js/
├── bin/
│   └── cli.js                    # CLI 진입점
├── src/
│   ├── index.ts                  # 메인 export
│   ├── cli.ts                    # CLI 로직
│   ├── analyzer/
│   │   ├── index.ts
│   │   ├── componentAnalyzer.ts  # 컴포넌트 분석
│   │   ├── functionAnalyzer.ts   # 함수 분석
│   │   └── hookAnalyzer.ts       # Hook 분석
│   ├── parser/
│   │   ├── index.ts
│   │   ├── astParser.ts          # AST 파싱 (Babel)
│   │   └── typeExtractor.ts      # TypeScript 타입 추출
│   ├── generator/
│   │   ├── index.ts
│   │   ├── testGenerator.ts      # 테스트 코드 생성
│   │   └── mockGenerator.ts      # Mock 생성
│   ├── templates/
│   │   ├── component.ejs         # 컴포넌트 테스트 템플릿
│   │   ├── function.ejs          # 함수 테스트 템플릿
│   │   └── snapshot.ejs          # 스냅샷 테스트 템플릿
│   └── utils/
│       ├── fileUtils.ts
│       └── naming.ts
├── templates/                    # 사용자 커스텀 템플릿
├── package.json
├── tsconfig.json
└── README.md
```

---

## 🤝 기여하기 (Contributing)

기여는 언제나 환영합니다! 다음과 같은 방법으로 기여할 수 있습니다:

1. 🐛 버그 리포트
2. 💡 기능 제안
3. 📝 문서 개선
4. 🔧 코드 기여

---

## 📄 라이선스 (License)

MIT License - 자유롭게 사용, 수정, 배포할 수 있습니다.

---

## 🙏 감사의 글 (Acknowledgments)

- [Babel](https://babeljs.io/) - AST 파싱
- [Jest](https://jestjs.io/) - 테스트 프레임워크
- [Testing Library](https://testing-library.com/) - 테스트 유틸리티

